<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Loan Duration:</strong>
  <%= @category.loan_duration %>
</p>

<%= link_to 'Edit', edit_category_path(@category), class: 'btn btn-info' %>
